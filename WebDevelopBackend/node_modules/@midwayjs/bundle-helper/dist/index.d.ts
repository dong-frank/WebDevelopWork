interface EntryGeneratorOptions {
    baseUrl?: string;
    rootDir?: string;
    ignore?: string[];
    pattern?: string[];
}
interface ExportCollection {
    configurationFilepath?: string;
    configurationClz?: string;
    exportFiles?: string[];
}
export declare const isWithin: (parent: string, child: string) => boolean;
export declare class EntryGenerator {
    private typeChecker;
    private program;
    private sourceDir;
    private programFiles;
    private isESM;
    DEFAULT_PATTERN: string[];
    DEFAULT_IGNORE_PATTERN: string[];
    BANNER: string;
    constructor(options?: EntryGeneratorOptions);
    private preset;
    private visit;
    collect(): ExportCollection;
    run(): void;
}
export {};
